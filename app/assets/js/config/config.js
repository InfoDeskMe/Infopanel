const CONFIG = {
    slideDelay: 3000,
    newsNumber: 4,
    tickerSpeed: 10,
    tickerOrientation: "toLeft",
    tickerOffset: "run-in",
    tickerMode: "chain",
    tickerHeight: "80",
    showTicker: 1,
    maxTickerNews: 3  
}

export default CONFIG